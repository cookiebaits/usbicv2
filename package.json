{
  "name": "usbicv2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",               // or "parcel src/index.html"
    "build": "vite build",       // creates a production `dist/`
    "lint": "eslint . && stylelint \"**/*.css\"",
    "format": "prettier --write \"**/*.{html,css,js,md}\"",
    "test": "cypress open"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-plugin-html": "^8.0.0",
    "stylelint": "^16.0.0",
    "prettier": "^3.3.0",
    "vite": "^5.4.0",
    "cypress": "^13.8.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.2.0"
  }
}
